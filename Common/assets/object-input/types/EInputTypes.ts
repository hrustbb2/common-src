export enum EInputTypes {
    text = 'text',
    string = 'string',
    array = 'array',
    file = 'file',
    image = 'image',
    color = 'color',
    bool = 'bool',
    select = 'select',
    // composite = 'composite',
}